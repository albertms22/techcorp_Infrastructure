# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ignore Mac .DS_Store files
.DS_Store

# Ignore vim swap files
*.swp
*.swo

# Ignore SSH keys - Policy: Specific SSH key filenames to prevent accidental 
# commit of private keys. Only specific key types are ignored, not all .pem/.pub files
# which could hide legitimate certificate files.
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
id_ecdsa
id_ecdsa.pub

# Keep tfvars.example
!terraform.tfvars.example

# Ignore plan output
plan_output.txt
*.plan

